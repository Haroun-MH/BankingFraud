# Root-level requirements — installs the full backend stack.
# Frontend dependencies are managed separately via npm (see frontend/package.json).
#
# Install with:
#   pip install -r requirements.txt

fastapi>=0.115.0
uvicorn>=0.30.0
mlflow>=2.13.0
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
psutil>=5.9.0
openpyxl>=3.1.0
pyarrow>=15.0.0
python-multipart>=0.0.9
optuna>=3.5.0
evidently>=0.4.0
scipy>=1.11.0
xgboost>=2.0.0
